package com.capstone.mobilestore.service;

import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;

import com.capstone.mobilestore.model.Mobile;

@FeignClient(name="MOBILE-SERVICE",fallbackFactory = MobileServiceFallbackFactory.class,url = "http://localhost:8081/swagger-ui/index.html#/mobile-controller")
public interface MobileConsumer {

	@GetMapping("/mobile/{id}")
	Mobile getMobileById(@PathVariable("id") Long mobileId);

}
